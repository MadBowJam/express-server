doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Profile
    body
        h1 Welcome to your profile, #{user.email}
        form(action="/logout", method="post")
            button(type="submit") Logout
