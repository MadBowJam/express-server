doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Users List
    body
        h1 Users List
        ul
          each user in users
              li #{user.name} - #{user.email}
